#!/bin/bash

set -e

source "$STATE"

if [ -z "$SKIP_TESTS" ] ; then
    cd $SRCDIR
    make testrunner
    make COVERAGE=true RM= all

    cd $SRCDIR/test
    ./gce.sh setup && eval $(./gce.sh hosts) && ./setup.sh
fi
